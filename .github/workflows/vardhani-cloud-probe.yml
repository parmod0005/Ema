name: VARDHANI Public Runner Probe

on:
  push:
    branches: [vardhani-cloud-probe]
    paths:
      - '.github/workflows/vardhani-cloud-probe.yml'

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-slim
    steps:
      - run: echo "Public GitHub slim runner OK"
